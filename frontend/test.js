const lines = [{qty: 10, rate: 490, taxPct: 0, freight: 0}, {qty: 100, rate: 100, taxPct: 18, freight: 0}]; let basic = 0; let tax = 0; let landed = 0; lines.forEach(l => { const q = Number(l.qty) || 0; const r = Number(l.rate) || 0; const t = Number(l.taxPct) || 0; const f = Number(l.freight) || 0; basic += (q * r); tax += (q * r * (t / 100)); landed += (q * (r + (r * (t / 100)) + f)); }); console.log({basic, tax, landed});
