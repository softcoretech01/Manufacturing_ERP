# Frontend environment (Vite). Copy to `.env` (or `.env.local`) and adjust.
# Only variables prefixed with VITE_ are exposed to the app.

# Backend API base URL (FastAPI). Default backend port is 8000.
VITE_API_BASE_URL=http://localhost:8000/api/v1
